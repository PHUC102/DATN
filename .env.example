# ===============================
# Database (MongoDB Atlas)
# ===============================
# Format: mongodb+srv://USER:PASS@CLUSTER/DB?retryWrites=true&w=majority
DATABASE_URL=

# ===============================
# App URLs
# - DEV (local):  http://localhost:3000
# - PROD (Vercel): https://your-app.vercel.app
# ===============================
NEXTAUTH_URL=
NEXT_PUBLIC_APP_URL=

# Ký JWT/Session của NextAuth (32–64 hex)
# Tạo nhanh: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NEXTAUTH_SECRET=

# ===============================
# VNPay (Sandbox/Production)
# Lưu ý:
# - VNP_URL: sandbox hoặc production tuỳ môi trường
# - VNP_RETURN_URL: TRANG UI để user quay lại (KHÔNG phải API),
#   ví dụ: https://your-app.vercel.app/checkout/result
# - Callback/IPN URL cấu hình trong cổng VNPay: https://<domain>/api/vnpay-callback
# ===============================
VNP_TMN_CODE=
VNP_HASH_SECRET=
VNP_URL=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
VNP_RETURN_URL=

# ===============================
# Google OAuth (nếu dùng)
# Cấu hình trên Google Cloud:
# - Authorized origins: https://<domain>
# - Redirect URI: https://<domain>/api/auth/callback/google
# ===============================
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ===============================
# SMTP (Gmail)
# - PORT=465 => SMTP_SECURE=true
# - PORT=587 => SMTP_SECURE=false
# - SMTP_PASS là App Password (không phải mật khẩu Gmail thường)
# ===============================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_NAME=G-cosmetic

# ===============================
# (Tuỳ chọn) Biến khác nếu dự án có
# ===============================
# FIREBASE_API_KEY=
# FIREBASE_AUTH_DOMAIN=
# ...

# ===============================
# KHÔNG DÙNG (Stripe) — để trống, không commit secret
# ===============================
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
